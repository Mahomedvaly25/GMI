# Regras do Projeto MGI

- Não realizar git push / deploy sem que o utilizador solicite explicitamente.
- Quando o utilizador pedir para fazer push/deploy, enviar **apenas** para a branch `develop`.
- **Nunca** fazer merge ou push para a branch `main` a menos que o utilizador solicite explicitamente o envio para a branch `main`.
